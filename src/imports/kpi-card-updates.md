Update the KPI summary cards at the top of the Integrity Dashboard.

Do NOT change:
• Card layout
• Icon positions
• Typography scale
• Card spacing
• Colors of status icons

Only modify the content structure.

--------------------------------------------------
REMOVE ELEMENTS
--------------------------------------------------

1. Remove the date label displayed at the bottom of each card:
   "Apr 2024"

2. Remove the percentage indicator shown at the top-right of each card
   (e.g., +4%, +8%, -3%, -5%).

The top-right area should remain empty. Do not replace it with anything.

--------------------------------------------------
UPDATE CARD TITLES
--------------------------------------------------

Current cards:

1️⃣ Total Tests Defined  
2️⃣ Tests Executed  
3️⃣ Tests Failed  
4️⃣ Critical Failures  

Change them to:

1️⃣ **Total Tests Defined** (keep as is)

2️⃣ **Tests Executed** (keep as is)

3️⃣ **Tests Passed**  
   (replace the current "Tests Failed")

4️⃣ **Tests Failed**  
   (replace the current "Critical Failures")

--------------------------------------------------
FINAL CARD ORDER
--------------------------------------------------

1️⃣ Total Tests Defined  
2️⃣ Tests Executed  
3️⃣ Tests Passed  
4️⃣ Tests Failed  

--------------------------------------------------
ICON RULES
--------------------------------------------------

Keep the existing icons but align them logically:

• Total Tests Defined → Document icon
• Tests Executed → Green check icon
• Tests Passed → Green success icon
• Tests Failed → Red warning / error icon

--------------------------------------------------
VISUAL STYLE
--------------------------------------------------

Maintain:
• Current card size
• Current typography
• Current spacing
• Current design system variables

Only remove the trend indicators and update the card labels.